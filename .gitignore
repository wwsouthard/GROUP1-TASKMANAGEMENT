# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Environment variables
# Note: server/.env is tracked temporarily so the course instructor can run the app.
# Prefer rotating Atlas credentials after the course if this repo is public.
client/.env

# Build outputs
dist/
client/dist/
server/dist/

# Test coverage
coverage/
client/coverage/
server/coverage/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS
.DS_Store
