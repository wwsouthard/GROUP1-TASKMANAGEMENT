/**
 * Sprint 1 — Application environment configuration.
 * apiBaseUrl is used by HTTP services; do not hardcode URLs in components.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000'
};
